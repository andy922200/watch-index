{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Watch Market",
  "description": "Brand-neutral market-specific watch display text, URLs, aliases, and nicknames without price data. Schema version 3 uses the brand's complete official reference as the canonical identity source.",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schemaVersion", "brandId", "collectedAt", "salesRegion", "marketCode", "locale", "watchCount", "source", "collectionAliases", "watches"],
  "properties": {
    "$schema": { "const": "../schemas/watch-market.schema.json" },
    "schemaVersion": { "const": 3 },
    "brandId": { "$ref": "#/$defs/slug" },
    "collectedAt": { "type": "string", "format": "date-time" },
    "salesRegion": { "type": "string", "minLength": 1 },
    "marketCode": { "$ref": "#/$defs/marketCode" },
    "locale": { "type": "string", "minLength": 2 },
    "watchCount": { "type": "integer", "minimum": 0 },
    "source": { "$ref": "#/$defs/source" },
    "collectionAliases": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "$ref": "#/$defs/slug" },
      "additionalProperties": { "$ref": "#/$defs/namesWithSources" }
    },
    "watches": { "type": "array", "items": { "$ref": "#/$defs/marketWatch" } }
  },
  "$defs": {
    "slug": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
    "marketCode": { "type": "string", "pattern": "^[A-Z]{2}$" },
    "reference": { "type": "string", "minLength": 1 },
    "watchId": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*:.+$" },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "url"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "url": { "type": "string", "format": "uri" }
      }
    },
    "publisherSource": {
      "type": "object",
      "additionalProperties": false,
      "required": ["publisher", "url"],
      "properties": {
        "publisher": { "type": "string", "minLength": 1 },
        "url": { "type": "string", "format": "uri" }
      }
    },
    "namesWithSources": {
      "type": "object",
      "additionalProperties": false,
      "required": ["names", "sources"],
      "properties": {
        "names": { "type": "array", "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
        "sources": { "type": "array", "uniqueItems": true, "items": { "$ref": "#/$defs/publisherSource" } }
      }
    },
    "marketWatch": {
      "type": "object",
      "additionalProperties": false,
      "required": ["watchId", "reference", "collectionName", "modelName", "caseDescription", "dialDescription", "isNewModel", "productUrl", "localNicknames"],
      "properties": {
        "watchId": { "$ref": "#/$defs/watchId" },
        "reference": { "$ref": "#/$defs/reference" },
        "collectionName": { "type": "string", "minLength": 1 },
        "modelName": { "type": "string", "minLength": 1 },
        "caseDescription": { "type": ["string", "null"] },
        "dialDescription": { "type": ["string", "null"] },
        "isNewModel": { "type": "boolean" },
        "productUrl": { "type": ["string", "null"], "format": "uri" },
        "localNicknames": { "$ref": "#/$defs/namesWithSources" }
      }
    }
  }
}
